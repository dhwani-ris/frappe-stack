---
description: Configure the plugin to talk to your Frappe site. Stores API key/secret, registers staging and production sites, points at your local config-repo checkout. Idempotent.
argument-hint: <site-url> [--prod]
---

# /frappe-stack:init

First command run on a new project. Idempotent — safe to re-run if config got out of sync (e.g. after a key rotation).

Nothing gets installed on the Frappe site. The plugin uses Frappe's stock REST API with token authentication.

## What it does

1. **Verify the site is reachable.** `curl <site>/api/method/ping` returns `pong`.
2. **Confirm version.** Refuse if Frappe < v15.
3. **Prompt for API key + secret.** Generated by you on the Frappe Desk: User → API Access → Generate Keys. Stored in your OS keychain (macOS Keychain / Windows Credential Manager / `pass` / etc.). Only a *reference* to the keychain entry lands in the local config file.
4. **Configure local plugin state** at `.frappe-stack/config.json`:
   ```json
   {
     "sites": {
       "staging": {
         "url": "https://staging.example.com",
         "api_key_secret_ref": "<keychain entry id>",
         "is_production": false
       },
       "prod": {
         "url": "https://prod.example.com",
         "api_key_secret_ref": "<keychain entry id>",
         "is_production": true
       }
     },
     "config_repo": {
       "url": "https://github.com/<org>/<config-repo>.git",
       "local_path": "/path/to/local/checkout",
       "branch": "main"
     }
   }
   ```
5. **Smoke-test:** call `GET /api/resource/DocType?limit_page_length=1` on the site; expect `200 OK`. Logs the test to `.frappe-stack/audit.jsonl`.

## Arguments

- `<site-url>` — required. Your Frappe site URL, e.g., `https://staging.example.com`.
- `--prod` — flag. Marks this as the production site. Without it, defaults to `staging`.

## Refuses if

- The site is < Frappe v15 (the plugin uses v15+ REST behaviors).
- The provided user lacks `System Manager` (the API key inherits the user's roles; without `System Manager`, most resource creates fail).
- Adding a `--prod` site when staging isn't configured yet (force order: staging first, prod second).

## Examples

```text
# First-time setup
/frappe-stack:init https://staging.example.com

# Add prod after staging is happy
/frappe-stack:init https://prod.example.com --prod

# Re-run after rotating the API key
/frappe-stack:init https://staging.example.com
```
